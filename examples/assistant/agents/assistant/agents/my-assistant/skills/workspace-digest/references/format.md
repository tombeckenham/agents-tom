# Digest format

Format the workspace digest as a short markdown list, largest files first when
it helps. Keep it under ~15 lines unless the user asks for the full listing.
Group by top-level directory when there are many files.
