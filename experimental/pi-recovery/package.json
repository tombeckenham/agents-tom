{
  "name": "@cloudflare/agents-pi-recovery",
  "description": "Internal genericity harness: a non-AI-SDK 'pi' adapter driving the shared chat-recovery engine (rfc-chat-recovery-foundation, Phase 5)",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types env.d.ts --include-runtime false",
    "test": "vitest run",
    "test:e2e": "vitest run --config e2e/vitest.config.ts"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "^0.80.2",
    "@earendil-works/pi-ai": "^0.80.2",
    "agents": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260729.1",
    "typescript": "^6.0.3",
    "vitest": "4.1.10",
    "wrangler": "^4.115.0"
  }
}
