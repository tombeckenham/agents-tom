{
  "name": "@cloudflare/agents-gateway-resume",
  "description": "Harness to verify AI Gateway native resumable streaming (cf-aig-run-id + /resume) across models",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "start": "wrangler dev --remote",
    "deploy": "wrangler deploy",
    "types": "wrangler types env.d.ts --include-runtime false"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^4.0.0",
    "@ai-sdk/openai": "^4.0.0",
    "ai": "^7.0.0",
    "zod": "^4.4.3"
  }
}
