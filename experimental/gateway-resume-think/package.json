{
  "name": "@cloudflare/agents-gateway-resume-think",
  "description": "Think Layer-B recipe: re-attach to an AI Gateway run (cf-aig-run-id) on DO eviction instead of regenerating",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "deploy": "wrangler deploy",
    "test": "vitest --run",
    "types": "wrangler types env.d.ts --include-runtime false"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^4.0.0",
    "@ai-sdk/openai": "^4.0.0",
    "@ai-sdk/provider": "^4.0.0",
    "@cloudflare/think": "*",
    "agents": "*",
    "ai": "^7.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260729.1",
    "typescript": "^6.0.3",
    "vitest": "4.1.10",
    "wrangler": "^4.115.0"
  }
}
