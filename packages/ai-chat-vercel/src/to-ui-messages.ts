/**
 * Moved to `agents/chat/agui-to-ui-messages` (it pairs with
 * `migrateUIMessageToAGUI` there, and `agents` is a real peer of every
 * consumer). Re-exported here for compatibility.
 */
export { toUIMessages } from "agents/chat";
