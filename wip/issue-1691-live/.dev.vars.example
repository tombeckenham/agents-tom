# Copy to .dev.vars and fill in the keys for the providers you want to test.
# Workers AI needs no key (uses your `wrangler login` account via the AI binding).
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
